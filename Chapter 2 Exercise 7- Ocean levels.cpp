#include <iostream>

int main()
{
	using namespace std;
	double current = 98;
	double year5 = 5 * 1.5;
	double year7 = 7 * 1.5;
	double year10 = 10 * 1.5;

	cout << "The number of millimeters higher than the current level that the oceans level will be in 5 years is ";
	cout << current + year5 << endl;
	cout << "The number of millimeters higher than the current level that the oceans level will be in 7 years is ";
	cout << current + year7 << endl;
	cout << "The number of millimeters higher than the current level that the oceans level will be in 10 years is ";
	cout << current + year10 << endl;

}

